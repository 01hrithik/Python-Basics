# Operators

-> here not assume share market's operators whom contral the volatility...
-> Programming world operators are help to mathematical performs and get result

-> i got it you also don't got it hence,
-> let it be simple you've 20 rupees and you want to eat two oreo biscuit packet and each oreo biscuit packet price is 20 rupees  
-> how much money you bring to shop to purchase two oreo biscuities.
-> you're answer will be 40 (u here added or multiply by (price,qunitity)(20 * 2 = 40) (20 + 20 = 40)) correct

-> so here types of operators which you don't requied to  remember just like while u do shopping.

$ Arithmetic operators :  +, -, *, ,/, %, ** (exponentiation), // (floor divisoon)
$ Assignment operators :  =, +=, -=, /=, *=, %= 
$ Comparison operators : ==, !=,>, <, <= , >= 
$ Logical operators : and , or, not
$ Identity operators :  is , is not
$ Membership operators : in, not in
$ Bitwise operators : and, or, xor, not, <<,>>

-> now i'm just mention the types operators 
-> next u can seen  code with example
-> operators is key which used according situattion current event
